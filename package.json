{
  "name": "specferret-workspace",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/core",
    "packages/cli"
  ],
  "scripts": {
    "test": "bun test",
    "build": "bun run --filter '*' build",
    "bootstrap:validation": "bun scripts/bootstrap-validation-repo.ts"
  }
}
