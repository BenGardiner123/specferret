{
  "name": "specferret-workspace",
  "private": true,
  "workspaces": [
    "packages/core",
    "packages/cli"
  ],
  "scripts": {
    "build": "npm run build --workspace=@specferret/core && npm run build --workspace=@specferret/cli",
    "test": "npm run test --workspace=@specferret/core && npm run test --workspace=@specferret/cli"
  }
}
